import { ClearNgforDirective } from './clear-ngfor.directive';

describe('ClearNgforDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearNgforDirective();
    expect(directive).toBeTruthy();
  });
});
